#!/usr/bin/env ruby

$:.unshift File.expand_path('../../lib', __FILE__)

require 'bundler'
Bundler.setup

require 'debugger'

require 'hobson/server'
Hobson::Model.redis = Redis.new

require 'irb'
IRB.start
